<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ripple_absent"
    android:radius="200dp">
    <!-- Mask to confine ripple within rounded rectangle -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
    <!-- Content with selected/normal states -->
    <item>
        <selector>
            <!-- Selected state - RED BACKGROUND -->
            <item android:state_selected="true">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/holo_red_light"/>
                    <stroke android:width="4dp" android:color="@android:color/holo_red_dark"/>
                    <corners android:radius="8dp"/>
                </shape>
            </item>
            <!-- Normal state - red outline only -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent"/>
                    <stroke android:width="3dp" android:color="@android:color/holo_red_light"/>
                    <corners android:radius="8dp"/>
                </shape>
            </item>
        </selector>
    </item>
</ripple>